name: Mass Scrape Ping

on:
  workflow_dispatch:
  workflow_run:
    workflows: ["Mass Scrape Wikipedia"]
    types: [completed]

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Wait and ping
        run: |
          echo "Mass scrape complete. Resting 15s..."
          sleep 15
          echo "Ping! Next scrape ready."
