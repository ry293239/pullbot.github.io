    - name: Run optimization
      timeout-minutes: 60
      run: |
        set -x
        echo "Python version:"
        python --version
        echo "Testing import..."
        python -c "from model import PullbotModel; print('Import OK')" 2>&1
        echo "Running optimize..."
        python src/model.py optimize 0.5 2>&1
        echo "Exit code: $?"
